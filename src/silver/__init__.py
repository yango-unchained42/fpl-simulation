"""Silver layer — cleaned, UUID-keyed data transformations.

Modules:
    fpl_stats       — FPL player performance + fantasy data
    understat_stats — Understat xG/xA/shots
    fixtures        — Cleaned fixture schedule
    unified_stats   — Merged FPL + Understat (main feature source)
    player_mapping  — Player identity resolution across sources
    team_mapping    — Team identity resolution across sources
"""
